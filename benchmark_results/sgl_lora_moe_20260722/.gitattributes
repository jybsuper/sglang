# Preserve profiler reports and raw tool exports byte-for-byte.  These files
# are evidence, not source text, and Git's whitespace checker otherwise tries
# to interpret some Nsight protobuf payloads as lines.
*.ncu-rep binary
*.nsys-rep binary
*.sqlite binary
*.log -text -diff
*.csv -text -diff
